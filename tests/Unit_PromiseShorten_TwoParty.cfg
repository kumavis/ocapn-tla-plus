\* Unit: positive witness that inter-vat promise-to-promise shortening
\* (notes/path-changes.md §1.2.b, Phase A) emits a two-party
\* op:resolve(targetRefId, desc:export-promise(refId)) on the resolver ->
\* listener wire when the listener is also the new promise's host.
\* Expected outcome: VIOLATION.  NoExportPromiseEmitted_MC asserts the
\* negation of what we want to demonstrate; TLC's counterexample IS the
\* witness trace.
\*
\* Wired in scripts/run-tests.sh with `expected = violation`.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT WireDescriptorContract_MC
INVARIANT OnlyKnownResolveDescriptors_MC
INVARIANT NoExportPromiseEmitted_MC
