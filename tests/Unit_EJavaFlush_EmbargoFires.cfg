\* Unit: positive witness that EJavaFlush's embargo gate fires at a non-head
\* chain hop when, and only when, a pre-resolve pipelined forward sits on the
\* wire to the resolver.  Expected outcome: VIOLATION.  The invariant
\* EmbargoNeverFires_MC asserts the negation of what we want to demonstrate;
\* TLC's counterexample IS the witness trace.
\*
\* Wired in scripts/run-tests.sh with `expected = violation`.  Regressions
\* that prevent embargo from firing on this minimal race surface this unit
\* test as a MISMATCH (it would start passing).
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT EmbargoNeverFires_MC
