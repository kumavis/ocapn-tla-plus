\* Unit: pipelining op:deliver-only through a 3PHO withdraw-promise.
\* HeadPeer = recipient pipelines on the chain ref whose localResolution
\* gets installed by op:resolve(desc:handoff-give); sends route through
\* the new RemotePromise(pw) to the target host's pre-minted LocalPromise.
\* Verifies the LocalPromise queue + ProcessPending drain correctly so
\* all messages reach the LocalTarget in FIFO order.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT NoMessageLost_MC
INVARIANT EndToEndRefFIFO_MC
INVARIANT GiftOneShot_MC
INVARIANT GiftHasOneRecipient_MC

PROPERTY EventualDelivery_MC
