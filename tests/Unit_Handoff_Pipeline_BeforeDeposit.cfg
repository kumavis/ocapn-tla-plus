\* Unit: pipelined op:deliver-only(refId=pw) reaches target host BEFORE the
\* matching op:deposit-gift (different wires).  Verifies the receive
\* handler blocks at the wire head until deposit pre-mints LocalPromise(pw),
\* then the message lands in LocalPromise.queue (never an ad-hoc buffer),
\* and ultimately drains to LocalTarget after withdraw resolves the promise.
SPECIFICATION Spec

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT NoMessageLost_MC
INVARIANT EndToEndRefFIFO_MC
INVARIANT GiftOneShot_MC
INVARIANT GiftHasOneRecipient_MC
INVARIANT WireDescriptorContract_MC
INVARIANT TwoPartyWireDescsOnly_MC

PROPERTY EventualDelivery_MC
