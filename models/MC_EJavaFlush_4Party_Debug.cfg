\* Debug: 4-party (ChainLength=3) EJavaFlush trace + lastAction for mermaid.
\* Loaded against models/MC_EJavaFlush_4Party.tla (the base module).
SPECIFICATION SpecDebug

CONSTANT DebugTrace = TRUE

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT NoMessageLost_MC
INVARIANT EndToEndRefFIFO_MC
\* Debug-only: forces a violation when the slow path (probe + ack +
\* ProcessHold drain) has completed AND every send is delivered, so TLC
\* renders a trace that exercises the slow path end-to-end.
INVARIANT NoSlowPathCompletion_MC
