\* Debug config for MC_OpFlushProtocol_3Party_PromiseShorten.tla.
\* Under the broader-trigger implementation the MC PASSES, so this
\* config has no counterexample to render -- it exists to enable
\* DebugTrace = TRUE for development runs against the same model.
SPECIFICATION SpecDebug

CONSTANT DebugTrace = TRUE

CHECK_DEADLOCK FALSE

INVARIANT TypeOK_MC
INVARIANT PairingInvariant_MC
INVARIANT NoMessageLost_MC
INVARIANT EndToEndRefFIFO_MC
